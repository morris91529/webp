import Button from '@mui/material/Button';

const button = () => {
    var output=[];
    output.push(<div><Button variant="contained" color="primary">sign in</Button></div>)
    return output;
}

export default button;